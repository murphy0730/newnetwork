# 供应网络风险控制塔（Supply Risk Control Tower）开发需求实现文档

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 文档版本 | V1.2 |
| 编写日期 | 2026-09-03（V1.1 更新于 2026-09-09，V1.2 更新于 2026-09-09） |
| 参考依据 | 项目前台 Demo：`index.html`（纯前端、内置模拟数据），本文档由对该文件的逆向分析得出；V1.1 融合《补充说明.md》（实际数据表结构与新增功能描述，见第 8 章）；V1.2 调整：供需计算由独立页签并入「供应网络总览」页（三口径切换 + 编码供需清单，见 3.1-3g/3h、3.6） |
| 适用读者 | 前端开发、后端开发、测试、实施 |
| 约定 | 文中所有代码标识符、字段名、枚举值均与 `index.html` 保持一致；标注「建议方案」的小节为 Demo 中不存在、由本文档推导补全的后台设计 |

---

## 2. 项目概述

### 2.1 业务背景

制造企业的供应网络由「成品 → 半成品 → 原材料/采购件」多级 BOM 构成，需求侧来自客户订单（PTO 按单生产）与周/月级预测，供给侧来自库存与任务令（MO，制造工单）的计划产出。计划员/调度员需要回答三个问题：

1. **缺不缺料**：未来 90 天内每个编码的供需净额是多少、哪天开始缺、缺多少（水位线/netting）；
2. **风险在哪**：哪些编码是独家供应（单一加工地）、哪些加工地历史交付延误严重、综合风险如何分级；
3. **出了异常会怎样**：某物料报废/延迟、某加工地停工时，影响会沿 BOM 传导到哪些成品、哪些客户订单。

本系统（控制塔）将上述计算可视化：一张可交互的供应网络图 + KPI 看板 + 异常推演 + 分析报告 + 数据导入 + 参数配置。

### 2.2 目标用户

- **计划员（planner）**：关注净缺口、短缺日期、需求对账（预测是否覆盖订单传导需求）；
- **调度员（dispatcher）**：关注加工地任务令、交期风险、停工影响；
- **供应风险管理人员**：关注独家供应、关键路径、风险榜单与热力图。

### 2.3 核心价值

- 多层级 BOM 的需求自动传导与供需净额（netting）计算，提前预警短缺日期；
- 独家供应、交期风险、净缺口三因素加权 + 一票否决的综合风险分级；
- Top-Down（BOM 传导 + 订单）与 Bottom-Up（自身预测）的需求对账，暴露预测缺口；
- What-if 异常推演：克隆数据快照重算，对比「前/后」风险变化，输出「谁 · 什么时候 · 短缺多少」；
- 纯配置化的阈值体系，保存即全量重算。

### 2.4 名词解释（以代码实际语义为准）

| 名词 | 说明 |
|---|---|
| 物料 / 编码（code） | 物料主数据表（`material`）中的一行，编码规则：成品 `F-*`、半成品 `S-*`、原材料/采购件 `R-*`。层级由 BOM 拓扑计算得出，不由编码前缀决定 |
| BOM（`bom`） | 父子件用量关系表：`parent`（父项编码）、`child`（子项编码）、`qty`（配比数量）、`site_constraint`（加工地约束）、生效/失效日期 |
| 任务令 / MO（`mo`） | 天级计划产出表（制造工单）。`due_date` 计划完工日期（承诺）、`sched_date` 排产预计产出时间、`actual_date` 实际完工日期（空=未完工）、`planned_date` 最初计划产出日期 |
| 订单（`orders`） | PTO 订单需求表。一张订单可有多行，每行直接引用一个编码（可为成品/半成品/采购件）+ 需求数量 + 计划交期 |
| 预测（`forecast_w` / `forecast_m`） | 周级预测（week=1~13）与月级预测（month=1~12）。**同一编码周预测优先于月预测**；周量均摊到 7 天、月量均摊到 30 天 |
| 净需求 / 净缺口（netGap） | 未来窗口（今天起 90 天）内，逐日累计「供给 − 需求」水位线的最低值；负值即缺口 |
| 水位线（water） | 从基准日 T0−30 天起逐日累加（供给−需求）形成的库存水位序列，长度为 121（区间 [T0−30, T0+90]） |
| 偏差率（devPct） | `netGap / 未来总需求 × 100`，风险分级的主要依据 |
| 可加工地（sites） | 某编码可生产的加工地集合。强信号=任务令中出现，弱信号=预测中出现，取并集 |
| 独家供应（exclusive） | 可加工地数量 = 1 的编码 |
| 关键路径（criticalPath） | 从某编码沿 BOM 向下（子件方向）累计生产周期（lead_time）最长的路径，即前置时间瓶颈段 |
| 需求对账（reconciliation） | Top-Down 需求（BOM 传导 + 直接订单）vs Bottom-Up 预测（自身预测）的 GAP，及 总需求 vs（库存+任务令产出）的计划 GAP |
| T0 | 计算基准日，取当天 0 点（`new Date(); setHours(0,0,0,0)`） |
| 纯供应/纯使用需求预测 | 预测量经表2「供应添加/使用剔除」修正后的供应/需求量（见 8.3、8.9） |
| 供需计算对象 | 底层编码在供需匹配中对应的上层编码集合，含父子、跨产业、最顶层三种口径（见 8.8） |
| 跨产业 | 制造部门（make_dept）不属于本产业映射表的编码；本产业范围为可配置信息（见 8.7） |

---

## 3. 功能需求总览

系统由 5 个页签组成（左侧导航 `data-tab`）：`overview`（供应网络总览）、`simulate`（异常推演）、`reports`（分析报告）、`data`（数据管理）、`config`（参数配置）。另有顶栏全局状态区。

### 3.0 顶栏与导航（全局）

| 功能点 | 描述 | 触发交互 | 展示内容 | 依赖数据 |
|---|---|---|---|---|
| 全局统计 | 顶栏常驻显示编码总数、BOM 边数、高风险数、数据计算时间 | 任意数据变更后自动刷新 | `tb-codes` / `tb-edges` / `tb-high` / `tb-time`；导航「分析报告」上的高风险角标 `nav-risk-count` | `dashboard()`、各表行数 |
| 重置数据 | 恢复内置初始化数据（演示数据集） | 点击「重置数据」按钮 | 清空筛选并重算全部指标 | `provider.reset()` |
| 页签切换 | 切换 5 个页面，更新标题与中英文面包屑 | 点击导航项 | 标题映射：总览/异常级联推演/分析报告/基表数据管理/参数配置 | 无 |
| URL 调试参数 | 支持 `?tab=`、`?run=simulate\|outage\|analyze`、`?code=`、`?open=` 直达指定功能 | 浏览器地址栏 | 自动切换页签并执行对应操作 | — |

### 3.1 页签一：供应网络总览（overview）

三栏布局：左「多维筛选」面板（默认收起）、中「KPI 指标条 + 供应网络图」、右「节点洞察」面板（默认收起）。

| # | 功能点 | 功能描述 | 触发交互 | 展示内容 | 依赖数据 |
|---|---|---|---|---|---|
| 1 | KPI 指标条 | 12 个指标卡：编码总数、严重/中等/轻微短缺、严重/中等/轻微过剩、独家供应、未分配加工地、综合高/中风险、高延误加工地；数字滚动动画（countUp，700ms） | 页面加载/数据变更；点击卡片将该指标条件写入筛选并刷新图（如点击「严重短缺」→ 过滤 `shortage=severe`）；「高延误加工地」不可点击，提示去报告页 | 指标卡 + 点击态高亮 | `dashboard()` |
| 2 | 多维筛选 | 产业、产品大类、调度员、计划员、加工地、层级（0 成品/1、2 半成品/3+ 原材料）、综合风险（high/moderate/low）、仅独家供应复选框、编码/名称模糊搜索 | 「应用筛选」「清除」按钮，搜索框回车 | 筛选面板（可折叠） | `dimensions()` 提供选项；`graphData(filters)` |
| 3 | 供应网络图 | 全量或筛选后的 BOM 网络图。节点=编码（或订单），边=BOM 配比 | 见下述工具栏 | ECharts graph 系列渲染，自定义布局 | `graphData()` / `searchGraph()` |
| 3a | 图工具栏 | 模式（全局 global / 约束 constrained）、布局（力导向 force / 分层 layered）、聚类（无/产品大类/加工地）、染色（层级/综合风险/加工地/无）、显示全部节点、指标/筛选/洞察面板开关 | 分段按钮点击即刷新 | — | 前端状态 `App.state` |
| 3b | 图显示设置 | 右下角齿轮弹层：显示顶部指标、风险光晕、独家供应双环、层级形状区分、层级大小区分、节点光晕、曲线边、下钻展开深度（1/2/3/5/完整BOM，默认 3） | 勾选开关即时生效 | 设置面板 `g-settings-panel` | `App.state.show`、`App.state.depth` |
| 3c | 图交互 | 悬停显示节点信息气泡（编码/名称/层级/大类/加工地/风险/独家标记）；滚轮缩放、拖拽平移、＋/－/自适应按钮；点击节点 → 打开洞察面板并加载详情，再次点击取消高亮 | 鼠标事件 | tooltip、缩放控件 `g-zoom`、节点/边数统计 `g-count` | `nodeDetail(code)` |
| 3d | 聚类凸包 | 聚类模式开启时，按组绘制凸包平滑闭合曲线（Andrew monotone chain + Catmull-Rom 样条，SVG overlay），组标签可拖拽整组移动 | 聚类模式切换；拖拽凸包描边/标签 | 半透明填充 + 虚线描边分组框 | `convexHull/padHull/smoothClosedPath` |
| 3e | 搜索下钻 | 搜索框输入：命中订单号/客户 → 订单供应网络视图；精确命中编码或名称 → 编码供应网络视图（上溯父件 + 下钻子件，限深度）；否则 → 按关键字过滤全图 | 回车/应用筛选 | 下钻视图出现「关键路径」「风险路径」高亮按钮与深度选择 | `searchGraph(q, depth)` |
| 3f | 路径高亮 | 下钻视图或选中节点后，可高亮「关键路径」（青色 #5eead4）或「风险路径」（金色 #fbbf24），非路径元素降透明 | 点击 `btn-hl-critical` / `btn-hl-risk`，再点取消 | 高亮态 | 视图数据中的 `criticalPath` / `riskPath` |
| 3g | 供需关系口径切换（V1.2） | 「供需网络（三口径）」卡片（位于原网络图卡片上方）顶部分段按钮：全量父子 / 跨产业 / 最顶层（8.8），切换后网络图按口径重算关系边并重绘（跨产业模式区分 本产业最顶层青实线 / 跨产业第一金虚线）；卡片内含预测版本（计划日期）下拉与月份选择 | 点击 `seg-scmode` | 关系边集随口径切换；`sc-mode-hint` 口径说明 | `SupplyCalc.compute()` 的 `edges` / `relCross` / `relTop` |
| 3h | 编码供需清单（V1.2） | 按口径差异化计算：全量父子=每编码一行，选中月 纯需求（自身预测+直接父项按用量传导−使用剔除）/纯供应（自身预测+外购添加+月初库存）/GAP；跨产业/最顶层=每「编码×计算对象」一行，对象需求/编码供应/GAP；点击清单行或图节点联动节点逐月明细（口径①明细含「上层传导」列） | 切换口径/月份；点击行 | 清单表 `sc-list-table` + 明细表 `sc-detail-table` | 8.9 计算结果（SupplyCalc） |
| 4 | 节点洞察面板 | 编码详情卡（见 4.3.5 节渲染内容）；若传入的是订单号则渲染订单详情卡 | 点击图节点；或报告/推演中的编码、订单链接（`App.openCode(code)`） | 右侧面板 `detail-panel` | `nodeDetail(code)` / `orderNetwork(orderNo)` |

### 3.2 页签二：异常推演（simulate）

| # | 功能点 | 功能描述 | 触发交互 | 展示内容 | 依赖数据 |
|---|---|---|---|---|---|
| 1 | 质量异常推演 | 输入异常节点编码（必填）、报废数量、延迟天数、异常原因；报废从该编码最近未完工任务令中扣减，延迟将该编码所有未完工任务令顺延 | 「▶ 执行质量异常推演」 | 影响报告卡片（见下） | `simulateQuality({code, scrapQty, delayDays, reason})` |
| 2 | 加工地停工模拟 | 选择加工地、停工天数、停工原因；该地所有未完工任务令顺延指定天数 | 「▶ 执行停工模拟」 | 同上 | `simulateOutage({site, days, reason})` |
| 3 | 影响报告 | 4 个影响指标（受影响节点/涉及任务令/涉及订单/异常参数）+ 结论文案 + 受影响订单标签（可点击下钻）+ 对比表（编码/名称/层级/原等级/现等级/偏差%前/偏差%后/预计短缺日期/综合风险，最多 100 行） | 推演完成后自动展示 `sim-result` | 「无受影响节点」时给出加大参数重试的提示 | 引擎克隆对比逻辑（见 6.9） |

### 3.3 页签三：分析报告（reports）

| # | 功能点 | 功能描述 | 触发交互 | 展示内容 | 依赖数据 |
|---|---|---|---|---|---|
| 1 | 顶层成品全链路分析 | 输入成品编码 → 一键展开供应树：4 项指标（节点数/最大层级/高风险节点/独家节点）+ ECharts 树图（LR 布局，按风险着色）+ 风险分布表 + 规则引擎建议动作（文案注明「AI 解读接口已预留标准 JSON」） | 「展开分析」/回车 | `ana-result` 区 | `topAnalysis(code)`、`getTree(code)` |
| 2 | 独家供应分析报告 | 可加工地=1 的编码清单：编码/名称/加工地/产业/交期风险/影响成品数/综合风险；按交期风险与影响面排序 | 进入页签自动加载 | `single-table` | `singleSourceReport()` |
| 3 | 加工地健康度报告 | 每加工地一行：风险节点数/独家供应数/高延误数/平均偏差%/任务令数/健康度（健康/关注/预警） | 同上 | `site-table` | `siteHealthReport()` |
| 4 | Top 20 风险榜单 | 按偏差率升序（最缺在前）的 Top 20：排名/编码/名称/产业/偏差%/净缺口/短缺日/独家/交期/综合；编码可点击下钻；可导出 CSV（带 BOM 头） | 同上；「导出 CSV」 | `top-table` | `topRisk(20)` |
| 5 | 风险热力图 | 编码 × 周次（W1~W13）× 周偏差% 热力图；取短缺 Top8 + 过剩 Top4 共 12 行；分段色阶（严重短缺红/中等橙/轻微黄/均衡绿） | 同上 | `heatmap-chart` | `heatmap(12)` |

### 3.4 页签四：数据管理（data）

| # | 功能点 | 功能描述 | 触发交互 | 展示内容 | 依赖数据 |
|---|---|---|---|---|---|
| 1 | 批量导入 | 拖拽/点击上传多个 CSV、xlsx、xls 文件，按表头自动识别表类型（命中 ≥min(2, 列数) 个中文列名即识别），逐张导入并重算 | 拖入 `drop-zone` 或选择文件 | 成功/失败计数消息；失败原因提示 | `detectTable()`、`parseImportFile()`、`importTable()` |
| 2 | 单表管理卡 | 6 张基表各一卡片：表名、字段说明、当前行数、「导入」「导出」「模板」按钮 | 点击按钮 | `import-grid` | `getMeta().counts` |
| 3 | 模板下载 | 下载带中文表头 + 1 行示例的 CSV 模板（带 BOM 头） | 「模板」 | 文件下载 | `TABLE_SCHEMAS` |
| 4 | 数据导出 | 将当前表数据导出为中文表头 CSV（带 BOM 头，字段含引号转义） | 「导出」 | 文件下载 | `exportTable(table)` |
| 5 | 恢复初始化数据 | 重置为内置种子数据 | 「恢复初始化数据」 | 全量重算 | `provider.reset()` |
| 6 | 手工维护表 | 表2 供应添加&使用剔除：宽表编辑器（固定三列 + 动态月份列，列可增删、行内编辑、组合唯一性校验），保存即全量重算；支持宽/长表导入、宽表导出、模板下载 | 「维护」「导入」「导出」「模板」按钮 | 手工维护卡（`manual-grid`）+ 全屏编辑覆盖层（`sa-mask`） | `supply_adjust` 长表、`afterDataChange()`（方案详见 8.11） |

> 注意：Demo 导入为**整表替换**语义（`importTable` 直接替换该表全部行）。

### 3.5 页签五：参数配置（config）

| # | 功能点 | 功能描述 | 触发交互 | 展示内容 | 依赖数据 |
|---|---|---|---|---|---|
| 1 | 短缺/过剩阈值 | 短缺 严重/中等/轻微、过剩 严重/中等/轻微 共 6 个偏差% 阈值 | 编辑数值 | 表单 `cfg-grid` | `config.shortage_levels` / `excess_levels` |
| 2 | 交期风险参数 | 高延误率阈值%、中延误率阈值%、样本量下限、质量合格率%（50~100） | 编辑 | 同上 | `config.delay`、`config.quality_yield` |
| 3 | 综合评级权重 | 净缺口权重、交期风险权重、独家供应权重、一票否决开关 | 编辑 | 同上 | `config.composite` |
| 4 | 需求口径 | 订单+预测 / 仅订单(PTO) / 仅预测 三选一 | 下拉选择 | 同上 | `config.demand_mode` |
| 5 | 保存/恢复默认 | 保存后立即全量重算；恢复默认=重置数据与配置 | 按钮 | 成功提示 | `setConfig()` / `reset()` |
| 6 | 跨产业映射 | 表6 make_dept 列表 + 「是否本产业」勾选，支持增删行、独立保存（8.7：可配置不写死），供需计算口径②判定依据 | 「+ 新增部门」「保存映射」 | 映射表 `im-table` | `industry_map` 表 |

### 3.6 供需计算（V1.2 起并入总览页）

> V1.1 新增独立页签（`data-tab="supply"`）；**V1.2 调整：不再独立成页签，整体并入 3.1「供应网络总览」页的「供需网络（三口径）」卡片**（位于原网络图卡片上方），旧 URL 参数 `?tab=supply` 重定向到 overview。基于实际业务数据表（表1/表2/表3/表4/表6 + 跨产业映射）的供需计算，由 `SupplyCalc` 模块实现，与总览演示引擎（Engine）数据隔离。

| # | 功能点 | 功能描述 | 触发交互 | 展示内容 | 依赖数据 |
|---|---|---|---|---|---|
| 1 | 预测版本选择 | 计划日期下拉，默认最新版本；切换即重算 | 下拉切换 `sc-version` | 版本统计（编码数 / BOM 边数 / 跨度月数） | `SupplyCalc.versions()` |
| 2 | 三口径供需网络图 | 分层网络图：节点含编码与选中月预测量/GAP（蓝=本产业、橙=跨产业、红描边=当月缺口）；边集随口径切换（全量父子 / 跨产业 / 最顶层，见 3.1-3g）；点击节点展开逐月明细 | 口径分段 `seg-scmode`；月份下拉 `sc-month`；点击节点 | ECharts graph（`sc-graph`）+ 明细表（`sc-detail-table`） | `supplyCompute(planDate)` |
| 3 | 编码供需清单 | 按口径差异化计算与展示（见 3.1-3h），选中月取值，按 GAP 降序；点击行联动节点逐月明细 | 切换口径/月份；点击行 | 清单表 `sc-list-table` | 8.9 计算结果 |
| 4 | 跨产业供需视图（累计跨度分析） | 口径② 配套明细：每个底层编码的「本产业最顶层对象」「跨产业第一对象」，月份为列展示逐月 GAP，累计 N 个月（3/6/11 可选）供应/需求/GAP | 累计跨度切换 `sc-cum-n` | 关系表（`sc-cross-table`） | `supplyCompute()` 的 `targets` |
| 5 | 数据入口 | 表1/3/4/6 经数据管理页 Excel/CSV 导入；表2 经手工维护卡（3.4-6）；跨产业映射经参数配置页（3.5-6） | 见对应页签 | — | `TABLE_SCHEMAS` 扩展 + `detectTable` |

---

## 4. 前台实现逻辑

### 4.1 技术栈

| 技术 | 用途 | 引入方式 |
|---|---|---|
| 原生 HTML/CSS/JS（ES2017+，无构建工具） | 整体应用，单文件交付 | — |
| ECharts（含 ZRender） | KPI 之外的全部图表：网络图（graph 系列 + 自绘坐标）、供需趋势折线、需求对账柱线混合图、热力图、供应树 tree 图 | 内嵌压缩库（index.html 887–934 行）；正式版建议改为 CDN/npm 依赖 |
| AntV G6 | 图可视化备选库（Demo 中主渲染已切换为 ECharts 自定义布局，`highlightPaths` 仍保留 G6 `updateData` 接口的兼容代码） | 内嵌压缩库（935–1016 行）；正式版可移除或仅用其一 |
| SheetJS（XLSX） | Excel（xlsx/xls）导入解析 | 内嵌压缩库（1017–1041 行） |

### 4.2 页面布局与设计规范

整体为深色控制塔风格，左侧固定导航（约 198px，1366px 以下 186px），右侧主区 = 顶栏 + 内容区；`body` 溢出隐藏，各面板内部滚动。响应式断点：1500px / 1280px / 1100px（1100px 以下隐藏洞察面板）。

**CSS 设计变量（`:root`）**：

- 基础色：`--bg #080d1a`、`--panel #0e1626`、`--panel-2 #111c31`、`--panel-3 #16233d`、线条 `--line #253a5f` / `--line-soft #1a2946`；
- 文字：`--ink #eef3fb`、`--ink-2 #b8c6de`、`--ink-3 #93a6c9`、`--ink-4 #7a8cb0`；
- 品牌：`--brand #3d8bff`、`--brand-2 #5eead4`，渐变 `--brand-grad`；
- 语义：`--danger #f43f5e`、`--warning #f59e0b`、`--ok #10b981`、`--info #3b82f6`、`--gold #fbbf24`；
- **风险语义色**：`--risk-high #f43f5e`、`--risk-moderate #f59e0b`、`--risk-low #10b981`、`--risk-nodata #64748b`；
- 其他：圆角 `--radius 12px` / `--radius-sm 8px`；数字字体 `--font-num: "JetBrains Mono", Consolas, monospace`；正文字体 PingFang SC / 微软雅黑等，字号 13.5px。

**图形用色（JS 常量）**：

- 图节点风险色 `RISK_COLORS`：`high #E5534B` / `moderate #E8A33D` / `low #5BBF8A` / `nodata #8A94A6`（红=高风险、绿=低风险）；
- 层级色 `LEVEL_COLORS`：订单(-1) 青 `#2DD4BF`、成品(0) 蓝 `#4A90E2`、半成品(1/2) 橙 `#E8A33D`、原材料(3+) 绿 `#5BBF8A`；
- 加工地色 `SITE_COLORS`：东莞 `#4A90E2`、深圳 `#2DD4BF`、松山湖 `#E8A33D`、武汉 `#9B6DF0`、成都 `#5BBF8A`；
- 聚类框调色板 `CLUSTER_PALETTE`：10 色循环。

**标签（tag）语义**：`tag.high`=红、`tag.moderate`=橙、`tag.low`=绿、`tag.nodata`=灰、`tag.exclusive`=金色（独家供应）、`tag.info`=蓝、`tag.severe`=深红。

### 4.3 各页签组件构成与渲染流程

#### 4.3.1 应用启动流程（`App.init()`）

1. `bindNav()` / `bindEvents()` 绑定导航与全部按钮事件；
2. `provider.getMeta()` 获取维度选项、各表行数、当前配置；
3. `fillFilters()` 填充筛选项与推演页加工地下拉；
4. `refreshTopbar()` → `refreshKPI()` → `refreshGraph()` → `loadDataPage()` → `loadConfigPage()`；
5. 默认收起筛选与洞察面板（`.ov` 加 `f-off d-off`）；
6. 处理 URL 调试参数；绑定 `resize` 重绘；
7. 启动前先 `getTopRisk(999)` 构建 `App._riskMap`（编码→综合风险，供树图着色）。

#### 4.3.2 总览页

- **KPI 渲染（`refreshKPI`）**：12 张卡片由 `dashboard()` 结果映射生成；点击有 `act` 的卡片将条件（如 `{shortage:"severe"}`、`{risk:"high"}`、`{exclusive:"yes"}`、`{unassigned:"yes"}`）写入 `state.filters` 并刷新图。
- **网络图渲染（`_renderEChartsGraph`）**：
  - 数据源：有 `filters.search` 时调 `searchGraph(q, depth)`，否则调 `graphData({...filters, siteMode})`；
  - 布局：`force`（自实现力导向模拟 `_forceSimulate`，含碰撞消解保证节点不重叠；聚类模式下有簇引力）或 `layered`（`_layeredPositions` / `_layeredTreePositions` 按层级分列网格排布，根在右、叶在左，同层按父节点排序减少交叉）；坐标经 `_fitToView` 适配视口与缩放系数；
  - 节点尺寸随数量自适应（`sizeScale`：≤40→1.15，≤100→1.0，≤250→0.85，≤600→0.72，否则 0.6）；**节点数 >1000 进入 LOD 降级**（小圆点、不显示悬浮详情、禁用聚类框）；
  - 染色优先级：聚类模式（节点统一淡蓝灰，颜色让给凸包框）> risk > site > none > level（默认）；
  - 显示设置控制：风险光晕（`gs-risk`）、独家双环（`gs-exclusive`，默认开）、层级形状（成品方/半成品圆/采购件菱形）、层级大小（默认开）、光晕（默认关）、曲线边（默认开）；
  - 交互：点击节点 `onNodeClick` → 打开洞察面板并 `getNodeDetail`；`graphroam`/滚轮后重绘凸包；缩放按钮调整 `_zoomScale` 后复用坐标重绘（`reusePositions`）。
- **悬停气泡（`_onNodeHover`）**：显示编码、名称、层级（订单/成品/半成品/采购件 + L 层级）、大类、加工地、风险（带色）、独家警示、生产周期；跟随鼠标并防溢出；同一节点去重。
- **路径高亮**：`toggleViewPath("critical"|"risk")` 与节点点击高亮互斥；全量视图无路径字段时回退到选中节点的关键路径。
- **深度下钻**：下钻视图（订单/编码）显示深度按钮组（1/2/3/5/完整BOM），切换后带 depth 重新请求。

#### 4.3.3 节点洞察面板（`renderDetail`）

从上到下渲染：

1. **头部**：编码、名称·产业·大类，标签组（综合风险/短缺等级/独家供应/交期风险/顶层成品）；
2. **三项指标**：净缺口、偏差率%（负红正绿）、预计短缺日；
3. **基础信息**：产业、大类、层级、计划员、调度员、生产周期、累计提前期（关键路径总长）、在排任务令数（`actual_date` 为空的 MO 数）；
4. **关键路径**：`code → code → …` 链路文案；
5. **风险说明**：`riskText(code)` 生成的自然语言；
6. **需求对账**（`_renderReconciliation`）：预测匹配卡（Top-Down=BOM传导+直接订单 vs Bottom-Up=自身预测，GAP 与状态标签）+ 计划匹配卡（总需求 vs 库存+任务令产出，计划 GAP 与状态标签）+ 周粒度堆叠柱图（BOM传导/直接订单堆叠 vs 自身预测堆叠 + 任务令产出折线，`renderReconChart`）；
7. **供应关系**：可加工地标签 + 强/弱信号说明 + 上游父件表（最多 6 行：编码 × 配比 × 加工地约束）+ 下游子件表（同）；
8. **关联订单**：订单号/客户/交期列表（含上游成品链引用的订单）；无则提示「可能由上层成品间接引用」；
9. **供需趋势图**（`renderTrendChart`）：日桶，前 60 天窗口的需求（红）/供给（绿）/库存水位（蓝，带面积）三条线；
10. **任务令表**（最多 10 行）：令号/加工地/数量/状态/承诺→实际日期。

若为订单（`renderOrderDetail`）：订单号、客户·交期·状态、风险节点数标签；指标（涉及编码/关键路径天数/风险节点数）；关键路径链路；风险路径段（编码+名称列表）；订单涉及编码表（最多 16 行）。

#### 4.3.4 异常推演页

两个表单卡片（grid2 双列）：质量异常（编码必填、报废数量默认 1000、延迟天数默认 7、原因默认「设备故障 / 质量事故」）；停工模拟（加工地下拉来自 dimensions、停工天数默认 15）。结果卡片默认隐藏，渲染：场景标题（类型·对象·原因）、4 个 impact 指标、message 文案、受影响订单标签（点击 `App.openCode(orderNo)` 下钻）、对比表（等级恶化标红、偏差变化着色）。空结果给出提示文案。

#### 4.3.5 报告页

进入页签时 `loadReports()` 串行加载四个报告。全链路分析需手动输入编码（默认空），结果显示：4 指标、树图（`renderTree`，由 `getTree` 的边集重建树，节点按 `_riskMap` 着色，初始展开 5 层）、建议动作列表、风险分布表。热力图 `visualMap` 分段：[-80,-50] 严重短缺红、(-50,-20] 中等橙、(-20,0] 轻微黄、(0,30] 均衡/过剩绿。Top20 导出 CSV 列：排名,编码,名称,产业,偏差%,净缺口,短缺日期,独家,交期风险,综合风险。

#### 4.3.6 数据管理页

`loadDataPage()` 渲染 6 张导入卡（`TABLE_SCHEMAS` 顺序：bom/material/inventory/mo/forecast_w/forecast_m，注意 demo 的导入卡 grid 不含 orders 表说明文案——`desc` 映射缺 orders 项，正式版应补齐）与顶部拖拽区。导入流程：`parseImportFile`（按扩展名选 parseCSV 或 XLSX 解析 → `normalizeRows` 中文表头转英文字段）→ `provider.importTable(table, rows)` → 更新行数 → `afterDataChange()`（重取 meta、重填筛选、刷新顶栏/KPI/图/数据页，若报告页激活则重载报告）。`detectTable` 通过表头匹配自动识别表类型。

#### 4.3.7 参数配置页

`loadConfigPage()` 将配置填入表单（质量合格率界面显示百分数，存储为小数）；`saveConfig()` 收集表单组装配置对象，`delay` 中未暴露的字段（`n_samples`、`high_lead_ratio`、`mid_lead_ratio`）从现有配置合并保留，提交后刷新顶栏/KPI/图。

### 4.4 前端状态管理与引擎调用方式

- **Provider 抽象层是前后端切换的唯一替换点**（index.html:2895–3117）。UI 只依赖 `provider`，不感知数据来源：
  - `LocalProvider`（当前 Demo）：内存持有 7 张表 + config，内部实例化 `Engine` 同步计算，所有方法包装为 async；
  - `RemoteProvider`：同接口、fetch 实现，接口路径见第 7.3 节；切换方式 `const provider = new RemoteProvider("/api")`（`USE_REMOTE=true`）。
- **全局状态** `App.state`：`filters`（当前筛选）、`siteMode`（global/constrained）、`layout`、`colorMode`、`cluster`、`depth`、`show`（6 个显示开关）、`meta`（维度/行数/配置）。
- **数据变更统一入口** `afterDataChange()`：任何导入/重置/配置保存后调用，保证全端一致重算。
- **消息提示** `msg(text, type)`：右上角浮层，3.4 秒自动消失；全局 `window.error` 兜底提示。

---

## 5. 数据模型

根据 `buildSeed()`（index.html:2425–2889）、`Engine._buildIndex()` 列名约定（1104–1111 行注释）与 `TABLE_SCHEMAS`（2907–2943）反推，共 **7 张基表**。所有表在 Demo 中为内存数组，正式版对应数据库表（见 7.2）。

### 5.1 `material` 物料主数据表

| 字段 | 类型 | 中文表头 | 含义 | 来源/说明 |
|---|---|---|---|---|
| code | string，主键 | 编码 | 物料编码；Demo 约定 F-=成品、S-=半成品、R-=原材料 | 必填 |
| name | string | 名称 | 物料名称 | 必填 |
| industry | string | 产业 | 如 ICT基础设施/智能终端/数字能源 | 必填；筛选维度 |
| category | string | 产品大类 | 如 服务器/交换机/智能手机/智能穿戴/光伏逆变器/储能电池包 | 必填；筛选维度 |
| planner | string | 计划员 | 负责计划员 | 筛选维度 |
| dispatcher | string | 调度员 | 负责调度员 | 筛选维度 |
| lead_time | number（天） | 默认生产周期（天） | 默认生产周期；实际周期优先从历史 MO 中位数计算，本字段为回退值（默认 7） | 关键路径、需求传导提前期 |
| safety_days | number（天） | 安全库存天数 | 安全库存天数（Demo 中仅展示，未参与 netting 计算） | — |
| moq | number | 最小起订量 | 最小起订量（Demo 中未参与计算） | — |

required：`code, name, industry, category`。

### 5.2 `bom` BOM 结构表

| 字段 | 类型 | 中文表头 | 含义 |
|---|---|---|---|
| parent | string | 父项编码 | 父件编码（上层） |
| child | string | 子项编码 | 子件编码（下层） |
| qty | number | 配比数量 | 单位父件所需子件数量 |
| site_constraint | string | 加工地约束 | 该 BOM 行的加工地约束，可空 |
| eff_date | date(YYYY-MM-DD) | 生效日期 | 生效日期（Demo 未参与过滤计算） |
| exp_date | date | 失效日期 | 失效日期（同上） |

required：`parent, child, qty`。联合唯一键建议 (parent, child, eff_date)。

### 5.3 `inventory` 库存表

| 字段 | 类型 | 中文表头 | 含义 |
|---|---|---|---|
| code | string，外键→material.code | 编码 | 物料编码 |
| site | string，可空 | 加工地 | **空 = 全局库存公共池**（约束模式下按各加工地需求比例分配） |
| onhand | number | 可用库存 | 现有可用量 |
| locked | number | 锁定库存 | 被锁定量；净可用 = onhand − locked |
| snapshot_time | date | 盘点时间 | 库存快照时间 |

required：`code, onhand, snapshot_time`。

### 5.4 `mo` 天级计划产出表（任务令/工单）

| 字段 | 类型 | 中文表头 | 含义 |
|---|---|---|---|
| mo_no | string，主键 | 任务令号 | 工单号，如 MO-10001 |
| code | string，外键 | 编码 | 产出物料编码 |
| site | string | 加工地 | 生产加工地（可加工地**强信号**来源） |
| due_date | date | 计划完工日期 | 承诺完工日期；供给落桶取 `sched_date || due_date` |
| qty | number | 计划产出数量 | 计划产出数量；入库量 × `quality_yield` |
| status | string | 排产状态 | 如 已排产/未排产（Demo 不参与计算，仅展示） |
| sched_date | date，可空 | 排产预计产出时间 | 排产预计产出日 |
| actual_date | date，可空 | 实际完工日期 | 空 = 未完工（在排）；交期风险统计只用有 actual_date 的历史单 |
| planned_date | date，可空 | 计划产出日期 | 最初计划日期；用于计算实际生产周期（actual − planned 中位数） |

required：`mo_no, code, qty`。

### 5.5 `forecast_w` 周级预测表

| 字段 | 类型 | 中文表头 | 含义 |
|---|---|---|---|
| code | string，外键 | 编码 | 物料编码 |
| site | string | 加工地 | 预测加工地（可加工地**弱信号**来源） |
| week | int，1~13 | 周次 | 相对周次（第 1 周 = T0 起第 1 个 7 天） |
| qty | number | 预测需求量 | 该周需求量，计算时均摊 /7 到每天 |

required：`code, site, week, qty`。

### 5.6 `forecast_m` 月级预测表

| 字段 | 类型 | 中文表头 | 含义 |
|---|---|---|---|
| code | string，外键 | 编码 | 物料编码 |
| site | string | 加工地 | 同上 |
| month | int，1~12 | 月份 | 相对月次（第 1 月 = T0 起第 1 个 30 天） |
| qty | number | 预测需求量 | 该月需求量，均摊 /30 到每天；**仅当该编码无周预测时生效** |

required：`code, site, month, qty`。

### 5.7 `orders` 订单需求表（PTO）

| 字段 | 类型 | 中文表头 | 含义 |
|---|---|---|---|
| order_no | string | 订单号 | 如 ORD-2026-101；一单多行 |
| customer | string | 客户 | 客户名称 |
| due_date | date | 计划交期 | 需求落桶日期 |
| status | string | 订单状态 | 如 已确认 |
| code | string，外键 | 编码 | 需求编码（可直接引用成品/半成品/采购件） |
| qty | number | 需求数量 | 需求数量 |

required：`order_no, code, qty`。主键建议 (order_no, code) 或独立行 ID。

### 5.8 表间关系

```
material 1 ──── n bom(parent) / bom(child)      （BOM 构成有向图，Kahn 拓扑分层）
material 1 ──── n inventory                      （按 code，可多加工地多行）
material 1 ──── n mo                             （按 code；mo.site 提供可加工地强信号）
material 1 ──── n forecast_w / forecast_m        （按 code+site；弱信号）
material 1 ──── n orders                         （按 code；orders.order_no 聚合为订单）
```

派生维度（不落库，引擎计算）：拓扑层级 level（成品=0）、可加工地 sites（强/弱信号并集）、独家标记、交期风险（code×site）、风险分级、需求/供给/水位序列。

---

## 6. 核心算法与业务规则（Engine 类）

Engine 构造签名：`new Engine(tables, config)`，`tables` 含 7 张表（数组或 `{rows}`），`config` 见 6.10。引擎内部以 T0=当天 0 点为基准，时间桶固定 `N=121` 个日桶（区间 [T0−30, T0+90]，偏移 `OFF=30`）；`HORIZON = config.horizon_days || 90`。所有派生结果惰性计算并缓存，`_invalidate()` 在数据/配置变更时清空全部缓存（分层、可加工地、独家、交期、风险、看板、净额、需求传导、对账等）。

**性能要点**：`_buildIndex()` 预建按 code 分组的索引（`_invByCode`、`_moByCode`、`_ordersByCode`、`_fcWByCode`、`_fcMByCode`）及按加工地索引（`_moBySite`），使 3 万编码规模下避免 O(N×M) 全表扫描。

### 6.1 拓扑分层 `topoLevels()`

- **输入**：bom、material。**输出**：`{ level: {code: int}, maxLevel, cycles }`。
- **算法**：Kahn 拓扑排序（BFS 分层）。入度 = 作为 child 被引用的次数；入度 0 的节点（无父件 = 成品）为 level 0，逐层向下递增。
- **环检测**：分层结束后仍未赋级的节点判为环上节点，统一置 `maxLevel`，`cycles` 计数。
- **层级语义**：0=成品，1/2=半成品，≥3=原材料（展示层映射）。

### 6.2 可加工地解析 `processableSites()`

- **输入**：mo、forecast_w、forecast_m。**输出**：`{ code: { sites, strong, weak, count } }`。
- **规则**：强信号 = 任务令（`mo`）中 `qty>0` 且 site 非空的记录；弱信号 = 周/月预测中 `qty>0` 的记录；`sites` = 强 ∪ 弱，`count` = 并集大小。独家判定（6.3）仅用该并集，注释明确「仅强信号参与；弱信号需人工确认」为评审口径。

### 6.3 独家供应判定 `exclusives()`

对每个编码：

- `count === 0` → 未分配加工地，note =「⚠️未分配加工地：该编码未出现在任何加工地的生产计划或预测中，请确认数据完整性」；
- `sites.length === 1` → `exclusive = true`，note =「独家供应：仅【X】可加工」；
- 否则非独家，note =「非独家：N 个加工地」。

### 6.4 交期风险 `deliveryRisk()`（按 code×site）

- **样本**：该编码+加工地最近 `cfg.delay.n_samples`（默认 20）条**已完工**（有 actual_date）任务令；单条延误天数 = `round((actual_date − commitDate)/天)`，其中承诺日期 `commitDate = sched_date || due_date`。
- **指标**：延误率 `rate = 延误条数/样本数 × 100`（延误 = delay>0）；平均延误 `avgDelay` = 延误条的平均天数（四舍五入）。
- **分级**（`lead = material.lead_time || 7`）：
  - `rate > cfg.delay.high_rate`（默认 30%）或 `avgDelay > lead × high_lead_ratio`（0.5）→ `high`；
  - 否则 `rate > mid_rate`（15%）或 `avgDelay > lead × mid_lead_ratio`（0.3）→ `moderate`；
  - 否则 `low`；
  - **样本量封顶**：样本数 < `min_samples`（默认 5）时等级最高只到 `moderate`；
  - **无样本** → `nodata`，提示「暂无历史交付数据，建议人工确认供应稳定性」。
- 输出 `{"code|site": { code, site, samples, rate, avgDelay, level }}`。

### 6.5 风险说明文本 `riskText(code)`

按以下优先级生成自然语言（供详情面板「风险说明」）：

1. 未分配加工地 → 数据完整性提示；
2. 独家且该地交期 high →「仅【X】可供应，且历史延误率【r%】，平均延误【d天】，存在独家供应+交期不稳定双高风险」；
3. 独家且交期稳定 →「仅【X】可供应，虽交期表现稳定，但需关注单点故障风险」；
4. 多加工地但存在 high 地 →「有【N】个加工地可供选择，但【X】历史延误率【r%】，建议优先使用其他加工地」；
5. 其余 →「有【N】个加工地可供选择，交期表现正常」。

### 6.6 需求传导 `_computeDemandAll()`（核心）

- **口径**（`cfg.demand_mode`，默认 `order_forecast`）：`order_forecast`=订单+预测、`order_only`=仅订单、`forecast_only`=仅预测。
- **独立需求落桶**（每个编码自身）：
  - 订单：按计划交期 `due_date` 落日桶；
  - 周预测：`week`∈[1,13] 截断，起点 = `OFF + (week−1)×7`，量 /7 均摊 7 天；
  - 月预测：仅当该编码**无周预测**时使用，`month`∈[1,12] 截断，起点 = `OFF + (month−1)×30`，量 /30 均摊 30 天。
- **BOM 自顶向下传导**：按拓扑层级升序处理每个编码；若该编码总需求 >0，对每条 BOM 子件行，将父件第 i 天的需求 × `qty` 移到子件第 `i − parentLT` 天（**提前期偏移**：`parentLT = _leadTimeFor(父件)`，即子件须提前父件生产周期天数就位）。
- **需求来源分解**：同时累计三个并行序列供对账——`srcOrder`（直接订单）、`srcForecast`（自身预测，Bottom-Up）、`srcBom`（上层 BOM 传导，Top-Down）。注意：传导基于「该编码总需求序列」（含订单+预测+上层传导）逐层向下传播。
- **`_leadTimeFor(code)`**：优先取该编码历史任务令 `actual_date − planned_date` 天数的中位数（样本 ≥2 才采用），否则回退 `material.lead_time`（默认 7）；结果缓存于 `_ltCache`。
- **输出**：每编码 `{ series[121], labels[121], off:30, srcOrder, srcForecast, srcBom }`；`demandBuckets(code, granularity)` 支持日/周（`_aggregate(series,7)`，12 桶 W1..W12）/月（`_aggregate(series,30)`，12 桶 M1..M12）聚合。

### 6.7 供给与滚动净额 `supplySeries()` / `netting()`

- **供给序列** `supplySeries(code, siteMode)`：
  - 期初库存放当日桶 `sup[OFF] = Σ(onhand − locked) × quality_yield`（`quality_yield` 默认 0.98）；siteMode=global 取全部库存行，约束模式只取该加工地行；
  - 未来任务令产出：按 `sched_date || due_date` 落桶，`qty × quality_yield`；约束模式只算该加工地的 MO。
- **单点净额** `_netOne(code, dem, sup, site)`：逐日累加 `water[i] += sup[i] − dem[i]` 形成水位线；在 `i ≥ OFF`（今天起）找最低水位 `minW` 及出现日；`netGap = minW`（若未来无桶则取期末累计值）；`totalDemand` = 未来窗口需求合计；`devPct = netGap/totalDemand × 100`（需求为 0 时为 0）；`shortageStart` = minW<0 时最低水位日的日期标签，否则 null。
- **全局模式** `netting("global")`：每编码一次 `_netOne`。
- **约束模式** `netting(site)`：按可加工地分别计算（无可加工地时用 `""`），净缺口取各地之和，水位线逐日加总，最差水位定短缺日；全局公共池库存（site 为空）由 `_allocateGlobalPool` 按各加工地需求比例分配。

### 6.8 需求对账 `demandReconciliation(code)`

- **未来窗口汇总**（i ∈ [OFF, N)）：`bomTotal`、`orderTotal`、`forecastTotal`、`demandTotal`；`topDown = bomTotal + orderTotal`，`bottomUp = forecastTotal`。
- **供给侧**：`inventory = sup[OFF]`（期初库存），`moOutput = 未来窗口供给合计 − inventory`，`totalSupply = inventory + moOutput`。
- **预测 GAP**：`forecastGap = topDown − bottomUp`，`forecastGapPct = forecastGap/topDown×100`；状态机：
  - topDown>0 且 bottomUp=0 → `no_forecast`（无自身预测）；
  - topDown=0 且 bottomUp>0 → `no_topdown_has_forecast`（仅自身预测）；
  - topDown=0 且 bottomUp=0 → `no_topdown`；
  - 双方均 >0：|gap%| ≤ 5 → `matched`（预测匹配）；gap% > 5 → `forecast_under`（预测不足）；gap% < −5 → `forecast_over`（预测过剩）。
- **计划 GAP**：`planGap = demandTotal − totalSupply`，`planGapPct = planGap/demandTotal×100`；状态机：`planGapPct < −50` → `severe_shortage`；< −20 → `moderate_shortage`；< 0 → `light_shortage`；=0 → `balanced`；> 0/20/50 → `light/moderate/severe_excess`。
- **周明细** `weeklyBreakdown`：13 周的 {week, bom, order, forecast, demand, supply}，供对账图。
- `demandReconciliationAll()` 为全编码汇总（缓存）。

### 6.9 风险分级 `riskLevels()`（每编码）

- **短缺/过剩分级**（基于全局模式 `devPct`，阈值见 6.10）：
  - `dev < shortage_levels.severe`（−50）→ 短缺 `severe`；< moderate（−20）→ `moderate`；< light（0）→ `light`；否则 `balanced`；
  - `dev > excess_levels.severe`（50）→ 过剩 `severe`；> moderate（20）→ `moderate`；> light（0）→ `light`。
- **交期等级**：取该编码所有可加工地中最差一级（high 3 > moderate 2 > low 1 > nodata 0）。
- **综合评分**：
  - `shortageScore = max(短缺分, 过剩分) × (dev<0 ? 1 : 0.6)`，其中 severe=3 / moderate=2 / light=1 / balanced=0；
  - `delayScore` = high 3 / moderate 2 / low 1 / nodata 0；
  - `exScore` = 独家 ? 2 : 0；
  - `score = shortageScore×w_shortage + delayScore×w_delay + exScore×w_exclusive`（默认权重 0.5/0.3/0.2）；
  - `score ≥ 2.5` → `high`；`≥ 1.5` → `moderate`；否则 `low`；
  - **一票否决**（`composite.veto`，默认启用）：短缺 `severe` 或（独家 且 交期 high）→ 直接 `high`。
- 输出每编码：`{ code, shortage, excess, delayLevel, exclusive, composite, score, devPct, netGap, totalDemand, shortageStart, riskText }`。

### 6.10 配置项 `DEFAULT_CONFIG`

```json
{
  "shortage_levels": { "severe": -50, "moderate": -20, "light": 0 },
  "excess_levels":   { "light": 0, "moderate": 20, "severe": 50 },
  "delay": { "n_samples": 20, "min_samples": 5, "high_rate": 30, "mid_rate": 15,
             "high_lead_ratio": 0.5, "mid_lead_ratio": 0.3 },
  "composite": { "w_shortage": 0.5, "w_delay": 0.3, "w_exclusive": 0.2, "veto": true },
  "quality_yield": 0.98,
  "horizon_days": 90,
  "exclusive_stable_weeks": 8,
  "demand_mode": "order_forecast"
}
```

> `exclusive_stable_weeks` 在 Demo 中定义但未参与计算，正式版可作为「独家供应稳定性观察周数」预留或移除。

### 6.11 统计看板 `dashboard()`

汇总输出字段：`total`、短缺三级计数（severeShortage/moderateShortage/lightShortage）、过剩三级计数、exclusive、unassigned、highComposite(=highRisk)、moderateRisk、highDelaySites（存在高延误编码的加工地数）、totalSites、finished/half/raw（层级 0 / 1~2 / ≥3 计数）、需求对账统计（reconMatched / reconForecastUnder / reconForecastOver / reconNoForecast / reconNoTopdown / reconPlanShortage / reconPlanExcess）。

### 6.12 图谱数据与搜索

- **`graphData(filters)`**：按 filters 过滤编码（industry/category/planner/dispatcher/risk/shortage/excess/unassigned/level/exclusive= yes|no/site/search 均支持），输出 `{nodes, edges, total}`；节点 data 含 label/name/industry/category/level/risk/devPct/exclusive/sites/shortage/delay，style 由 `_nodeStyle` 生成（风险填充色、独家金色描边 3px、层级尺寸 30/24/20/16）；边 = 两端均在筛选集内的 BOM 行。
- **`nodeDetail(code)`**：见 4.3.3；数据含基础信息、level、sites（含强弱信号）、exclusive 及 note、risk 全量、累计提前期与关键路径、关联订单、moCount、trend（labels/demand/supply/water）、parents/children（各含 qty 与 site_constraint）、mos、reconciliation。
- **`ordersByCode(code)`**：该编码及其全部上游（父件方向，直至成品）编码被引用的订单去重列表 `{order_no, customer, due_date, status}`。
- **`criticalPath(code)`**：递归（带 memo）沿子件方向找「自身 lead_time + 最深子链累计 lead_time」最大的路径，输出 `{path:[], totalLead}`；`_criticalPathIn(code, all)` 为限定节点集版本。
- **`orderNetwork(orderNo, depth)`**：订单节点（level=-1，青色边指向引用编码）+ 引用编码 + 各编码向下（子件方向，限 depth，`all`=不限）BOM 展开 + 集合内最长关键路径 + 风险路径（关键路径上 composite 为 high/moderate 或独家的节点）；输出 message 摘要。
- **`codeNetwork(code, depth)`**：该编码的上游父件链 + 下游子件树的连通子图（限 depth）+ 关键路径 + 风险路径。
- **`searchGraph(q, depth)`**：q 命中订单号（包含匹配）或客户 → `orderNetwork`（取首个命中）；精确等于编码或名称包含 → `codeNetwork`（取首个）；否则 `graphData({search:q})`。
- **深度归一化** `_normalizeDepth`：`"all"/null/""/NaN/负数 → -1（不限）`，否则取正整数；BFS 限深展开（`_upstreamSetDepth`/`_downstreamSetDepth`）。

> 术语提醒：Demo 中 `_upstreamSet` 沿 `_parents`（子件→父件，朝成品方向）展开，`_downstreamSet` 沿 `_children`（父件→子件，朝原材料方向）展开；文档与代码保持一致。

### 6.13 异常推演（克隆对比逻辑）

- **`simulateQuality({code, scrapQty, delayDays, reason})`**：
  1. 复制全部 MO 行；报废量从该编码**未完工**任务令逐条扣减（单条最多扣到 0），直至扣完；
  2. `delayDays>0` 时该编码所有未完工任务令的 `sched_date`（无则 `due_date`）顺延；
  3. `_cloneWithMo(mos)` 用替换后的 MO 表 + 其余原表 + 深拷贝配置构造新 Engine（T0 对齐），重算 `riskLevels()`；
  4. 仅检查异常节点的上游集合（含自身，即朝成品方向）；**恶化判定**（满足其一即受影响）：短缺等级上升 / 综合等级上升 / 短缺开始日提前或新出现 / |ΔdevPct| > 3；
  5. 受影响集排序：短缺等级 → 综合等级 → |Δdev|；截取前 100 条；`_impactStat` 统计涉及的未完工任务令数与关联订单；
  6. 输出 `{scenario, affectedCount, affected[], affectedMoCount, affectedOrders[], message}`；无影响时 message 提示加大参数重试。
- **`simulateOutage({site, days, reason})`**：该加工地全部未完工任务令顺延 `days` 天，克隆重算后对**全部编码**做同样的恶化对比；输出增加 `shiftedMo`（顺延任务令数）。
- **`_cloneWithMo(mos)`**：克隆要点——仅替换 mo 表，配置深拷贝，T0 与原引擎一致（保证日期桶对齐）。

### 6.14 报告算法

- **`topAnalysis(code)`**：下游全集（子件方向）展开；输出 totalNodes、maxLevel、riskDist（high/moderate/low/nodata 计数）、exclusiveList（独家节点：编码/名称/加工地/交期等级）、bottleneck（从根沿「短缺最深（devPct 最小）且 shortage 为 severe/moderate 的子件」贪心向下，最长 12 节点）、suggestions（规则模板：独家节点>0 → 建议二供/备货（列前 5）；severe → 紧急补货/调拨（附短缺开始日）；moderate → 提前采购；附瓶颈路径；最多 12 条）、timestamp。
- **`singleSourceReport()`**：所有独家编码，含 site、industry、category、delay、risk、affectedFinished（上游链中 level=0 的成品数）、affectedCodes（上游集合大小）、note；排序：交期等级（high→moderate→low→nodata）再按影响成品数降序。
- **`siteHealthReport()`**：每加工地：riskNodes（该地可加工编码中综合 high 数）、exclusive（独家且唯一地为该地的编码数）、avgDev（该地编码平均 devPct，保留 1 位）、delayHigh（该地高延误编码数）、moCnt（任务令数）；健康度 = riskNodes=0「健康」/ ≤2「关注」/ 否则「预警」；按 riskNodes 降序。
- **`topRisk(n=20)`**：全部编码按 devPct 升序取前 n（最缺在前）。
- **`heatmap(topN=12)`**：短缺 Top8（devPct<0 中 devPct 最小者）+ 过剩 Top4（devPct≥0 中最大者），按 devPct 升序；每编码 13 周，每周取「周内最低水位 / 周内需求 × 100」（周需求为 0 则 0），保留 1 位小数；输出 `{rows:[{code:"编码\n名称", cells[13]}], weeks:["W1".."W13"]}`。
- **`trend(code)`**：`{labels, demand, supply, water}` 日桶序列（同 6.6/6.7）。
- **`dimensions()`**：industries/categories/planners/dispatchers（物料主数据去重）+ sites（全部可加工地并集），供筛选器。

---

## 7. 后台实现逻辑（建议方案）

> Demo 无后台；本章为正式实现的设计建议。接口契约以 `RemoteProvider`（index.html:3083–3112）为准——Demo 前端已内置该契约，后端按此实现即可无缝切换（`USE_REMOTE=true`）。Engine 注释亦提示「列名约定与后端 store.py 的英文列一致」，即原始设计意图为 Python 后端。

### 7.1 推荐架构

```
┌────────────┐   HTTP/JSON   ┌──────────────┐   读取    ┌──────────────┐
│ 前端 SPA    │ ───────────→ │ API 服务      │ ───────→ │ 数据库        │
│ (现有 UI)   │              │ (REST + 鉴权) │          │ (7 张基表+配置)│
└────────────┘              └──────┬───────┘          └──────────────┘
                                   │ 调用
                            ┌──────┴───────┐
                            │ 计算引擎服务   │  ← 与前端 Engine 算法一一对应（第 6 章）
                            │ (内存快照+缓存)│
                            └──────────────┘
```

- **API 服务**：鉴权、参数校验、数据 CRUD、Excel 导入导出、调用引擎并返回结果；技术栈不限（Python FastAPI / Node NestJS 均可，Demo 注释暗示 Python）。
- **计算引擎**：将第 6 章算法用服务端语言等价实现；以「数据快照 + 配置版本」为键缓存计算结果（见 7.4）。
- **数据库**：持久化 7 张基表与配置；支持导入整表替换。

### 7.2 数据库表结构设计（与第 5 节一一对应）

以下以通用 SQL 表述（字段类型可按 MySQL/PostgreSQL 调整）：

```sql
CREATE TABLE material (
  code        VARCHAR(64)  PRIMARY KEY,
  name        VARCHAR(255) NOT NULL,
  industry    VARCHAR(64)  NOT NULL,
  category    VARCHAR(64)  NOT NULL,
  planner     VARCHAR(64),
  dispatcher  VARCHAR(64),
  lead_time   INT          DEFAULT 7,
  safety_days INT          DEFAULT 7,
  moq         DECIMAL(18,3) DEFAULT 0
);
CREATE INDEX idx_material_industry ON material(industry);
CREATE INDEX idx_material_category ON material(category);

CREATE TABLE bom (
  id              BIGINT PRIMARY KEY AUTO_INCREMENT,
  parent          VARCHAR(64) NOT NULL,
  child           VARCHAR(64) NOT NULL,
  qty             DECIMAL(18,6) NOT NULL,
  site_constraint VARCHAR(64) DEFAULT '',
  eff_date        DATE,
  exp_date        DATE,
  UNIQUE KEY uk_bom (parent, child, eff_date),
  KEY idx_bom_child (child),
  KEY idx_bom_parent (parent),
  FOREIGN KEY (parent) REFERENCES material(code),
  FOREIGN KEY (child)  REFERENCES material(code)
);

CREATE TABLE inventory (
  id            BIGINT PRIMARY KEY AUTO_INCREMENT,
  code          VARCHAR(64) NOT NULL,
  site          VARCHAR(64) DEFAULT '',   -- 空=全局公共池
  onhand        DECIMAL(18,3) NOT NULL,
  locked        DECIMAL(18,3) DEFAULT 0,
  snapshot_time DATE NOT NULL,
  KEY idx_inv_code (code),
  FOREIGN KEY (code) REFERENCES material(code)
);

CREATE TABLE mo (
  mo_no        VARCHAR(64) PRIMARY KEY,
  code         VARCHAR(64) NOT NULL,
  site         VARCHAR(64),
  due_date     DATE,
  qty          DECIMAL(18,3) NOT NULL,
  status       VARCHAR(32),
  sched_date   DATE,
  actual_date  DATE,
  planned_date DATE,
  KEY idx_mo_code (code),
  KEY idx_mo_site (site),
  KEY idx_mo_actual (actual_date),        -- 交期风险统计只查已完工单
  FOREIGN KEY (code) REFERENCES material(code)
);

CREATE TABLE forecast_w (
  id   BIGINT PRIMARY KEY AUTO_INCREMENT,
  code VARCHAR(64) NOT NULL,
  site VARCHAR(64) NOT NULL,
  week TINYINT NOT NULL,                  -- 1~13
  qty  DECIMAL(18,3) NOT NULL,
  UNIQUE KEY uk_fw (code, site, week),
  FOREIGN KEY (code) REFERENCES material(code)
);

CREATE TABLE forecast_m (
  id    BIGINT PRIMARY KEY AUTO_INCREMENT,
  code  VARCHAR(64) NOT NULL,
  site  VARCHAR(64) NOT NULL,
  month TINYINT NOT NULL,                 -- 1~12
  qty   DECIMAL(18,3) NOT NULL,
  UNIQUE KEY uk_fm (code, site, month),
  FOREIGN KEY (code) REFERENCES material(code)
);

CREATE TABLE orders (
  id       BIGINT PRIMARY KEY AUTO_INCREMENT,
  order_no VARCHAR(64) NOT NULL,
  customer VARCHAR(255),
  due_date DATE,
  status   VARCHAR(32),
  code     VARCHAR(64) NOT NULL,
  qty      DECIMAL(18,3) NOT NULL,
  KEY idx_orders_no (order_no),
  KEY idx_orders_code (code),
  FOREIGN KEY (code) REFERENCES material(code)
);

CREATE TABLE app_config (                 -- 单行配置表（JSON 存储，键固定 'global'）
  cfg_key   VARCHAR(32) PRIMARY KEY,
  cfg_value JSON NOT NULL,
  updated_at DATETIME
);
```

索引要点：`bom(parent)`/`bom(child)` 支撑拓扑与双向 BFS；各业务表 `code` 索引支撑引擎分组索引；`mo(site)` 支撑停工模拟与健康度。

### 7.3 RESTful API 清单

以下路径与 Demo `RemoteProvider` 完全一致。除导入/导出外均返回 JSON；导入为整表替换语义。

| # | 方法 | 路径 | 请求参数 | 响应结构 | 说明 |
|---|---|---|---|---|---|
| 1 | GET | `/api/meta` | 无 | `{ dimensions: {industries[],categories[],planners[],dispatchers[],sites[]}, counts: {bom:n, material:n, ...}, config }` | 维度选项+各表行数+当前配置 |
| 2 | GET | `/api/dashboard` | 无 | 见 6.11 全部字段 | 风险看板统计 |
| 3 | GET | `/api/graph?f=<urlencoded JSON>` | f = filters 对象（industry/category/planner/dispatcher/risk/shortage/excess/unassigned/level/exclusive/site/search/siteMode） | `{ nodes:[{id,data,style}], edges:[{id,source,target,data,style}], total }` | 网络图数据（6.12） |
| 4 | GET | `/api/search?q=&depth=` | q 关键字；depth=1/2/3/5/all | orderNetwork 或 codeNetwork 或 graphData 结构 | 智能搜索（6.12） |
| 5 | GET | `/api/orders/{order_no}/network?depth=` | depth | `{ orderNo, customer, dueDate, status, lines, nodes, edges, totalNodes, criticalPath:{path,totalLead}, riskPath[], message }` | 订单供应网络 |
| 6 | GET | `/api/nodes/{code}` | 无 | 节点详情全量对象（4.3.3：含 risk、reconciliation、trend、parents/children、mos、orders 等） | 物料详情；404 当编码不存在 |
| 7 | GET | `/api/nodes/{code}/paths` | 无 | `{ up:[code...], down:[code...] }` | 上/下游集合（高亮用） |
| 8 | GET | `/api/nodes/{code}/tree` | 无 | `{ nodes:[{id,...style}], edges:[{source,target,qty}], count }` | 供应树（报告页树图） |
| 9 | GET | `/api/analysis/{code}` | 无 | topAnalysis 结果（6.14） | 顶层成品全链路分析 |
| 10 | GET | `/api/reconciliation/{code}` | 无 | demandReconciliation 结果（6.8） | 单编码需求对账 |
| 11 | GET | `/api/reconciliation/all` | 无 | 数组，元素同 #10 | 全量对账汇总 |
| 12 | GET | `/api/trend/{code}` | 无 | `{ labels[], demand[], supply[], water[] }` | 供需趋势（日桶 121 点） |
| 13 | GET | `/api/reports/heatmap` | 无 | `{ rows:[{code,cells[13]}], weeks[13] }` | 风险热力图 |
| 14 | GET | `/api/reports/top-risk?n=20` | n | topRisk 数组（6.14） | 风险榜单 |
| 15 | GET | `/api/reports/single-source` | 无 | `{ total, rows[] }`（6.14） | 独家供应报告 |
| 16 | GET | `/api/reports/site-health` | 无 | 数组（6.14） | 加工地健康度 |
| 17 | POST | `/api/simulate/quality` | body `{code, scrapQty, delayDays, reason}` | 6.13 输出 | 质量异常推演（不落库） |
| 18 | POST | `/api/simulate/outage` | body `{site, days, reason}` | 6.13 输出 + shiftedMo | 停工模拟（不落库） |
| 19 | POST | `/api/import/{table}` | body `{rows:[...]}`，rows 为英文字段行对象 | `{ rows:n, table, counts }` | 整表替换导入；table ∈ 7 张表名 |
| 20 | GET | `/api/export/{table}` | 无 | CSV 文本（中文表头，含引号转义） | 导出单表；建议 Content-Type: text/csv |
| 21 | POST | `/api/reset` | 无 | `{ ok:true }` | 恢复初始化数据（正式版可改为「恢复到最近备份/基线快照」） |
| 22 | GET | `/api/config` | 无 | config 对象（6.10） | 读取配置 |
| 23 | PUT | `/api/config` | body = config 对象（允许部分字段，服务端合并） | `{ ok:true }` | 保存配置并触发重算/失效缓存 |

统一错误格式建议：`{ error: { code, message, details? } }`，HTTP 状态码语义化（400 参数错、404 不存在、422 校验失败、500 服务异常）。

### 7.4 服务端计算引擎职责与性能要求

- **职责划分**：引擎无状态化设计——每次计算以「7 表快照 + config」构造计算上下文；API 层负责持久化，引擎只负责计算；模拟接口在内存快照副本上计算（对应 `_cloneWithMo`），不写库。
- **缓存策略**：
  - 以 `(数据版本号, config 哈希)` 为键缓存派生结果（分层、风险分级、看板、对账汇总等，对应 `_invalidate` 清空的那些缓存）；导入/配置保存/reset 时数据版本号 +1，全部缓存失效；
  - 重计算（如 `riskLevels()` 全量）建议异步预计算 + 结果落缓存，看板/报告类接口直接读缓存；单编码详情/趋势按需计算；
  - 模拟推演不复用主缓存，每次独立计算。
- **性能目标（建议）**：Demo 注释以 3 万编码为设计规模——该规模下 `graphData`/`riskLevels` 全量计算 ≤ 3s（服务端），看板/报告读缓存 ≤ 300ms；单编码接口 ≤ 500ms；推演接口 ≤ 5s。前端 >1000 节点自动 LOD 降级，后端 `graphData` 建议支持分页/采样上限参数（正式版扩展，Demo 无）。
- **并发**：导入与配置保存需互斥（避免半更新快照参与计算）；模拟接口只读快照，天然可并发。

### 7.5 Excel/CSV 导入校验规则与错误处理（建议方案）

Demo 前端已做的：扩展名白名单（csv/xlsx/xls）、中文表头→英文字段映射（`normalizeRows`，中英表头均可识别）、自动表识别（表头命中 ≥min(2, 列数) 个列名）、CSV 引号/逗号转义解析。

服务端应补充：

1. **表头校验**：缺失 `required` 列（见第 5 节各表 required）→ 422，报缺失列名；
2. **行级校验**：
   - 必填字段为空 → 记录行号与原因；
   - 数值字段（qty/onhand/locked/lead_time/week/month 等）非数字 → 报错；week∉[1,13]、month∉[1,12] → 报错（引擎会截断，但导入期应显式拦截）；
   - 日期字段无法解析为 YYYY-MM-DD → 报错；
   - **外键完整性**：bom.parent/child、inventory.code、mo.code、forecast.code、orders.code 必须存在于 material 表（本次同事务导入的 material 也算）；
   - BOM 环检测：导入后可运行 `topoLevels`，`cycles>0` 时告警（不强制拒绝，返回警告列表）；
3. **错误处理策略（建议）**：默认「严格模式」——任一行错误则整表拒绝并返回全部错误明细（行号+字段+原因，上限 100 条）；可选「宽松模式」——跳过错误行导入其余，响应附错误清单；
4. **事务性**：整表替换须在单事务内完成（删旧插新），失败回滚；
5. **大小限制**：单文件建议 ≤ 20MB / ≤ 50 万行，超限 413；
6. 导入成功后数据版本号 +1，触发全量重算（可异步）。

---

## 8. 实际业务数据表

### 8.1 与 Demo 数据模型的对应关系

| 实际表 | Demo 对应 | 差异要点 |
|---|---|---|
| 表1 需求预测表 | forecast_w / forecast_m | 实际为「按版本（计划日期）的月度预测」，预测月份为绝对月份（DATE），需按编码+计划日期+预测月份聚合；Demo 为相对周次/月次 |
| 表2 供应添加&使用剔除表 | 无（新增） | 手工维护，对供应方/使用方预测量做月度增减修正 |
| 表3 BOM 基表 | bom | 字段语义一致（父项/子项/子项单位用量 ≈ parent/child/qty）；明确无自环与循环依赖 |
| 表4 库存表（库存拍照宽表） | inventory | 实际为按日拍照的宽表，需按子库类型过滤、按创建日期选月初/实时快照 |
|                            |                         |                                                              |
| 表6 制造属性维护基表 | material 的产业维度 | 以 make_dept（制造部门）识别产业；跨产业映射须可配置 |

### 8.2 表1 需求预测表

表结构：

| 字段名 | 数据类型 | 说明 |
|---|---|---|
| 计划日期 | DATE | **关键字段**：上传的预测版本日期（每周上传一个最新预测版本） |
| 周月预测 | VARCHAR | 如「月」；仅保存展示 |
| 类型 | VARCHAR | **关键字段**：编码类型（如 半成品） |
| 编码 | VARCHAR | **关键字段** |
| 供应商名称 / 供应商代码 | VARCHAR | 仅保存展示 |
| 加工地名称 / 加工地代码 | VARCHAR | 仅保存展示 |
| 组织ID | INTEGER | 仅保存展示 |
| 组织代码 / 产品线 / 产品族 / 加工对象包 | VARCHAR | 仅保存展示 |
| 预测所在年月 | DATE | **关键字段**：预测起始月份 |
| 预测月份 | DATE | **关键字段**：预测数据对应的月份（一般从起始月向后跨度 11 个月） |
| 预测数量 | DECIMAL | **关键字段**：预测值，即后续计算使用的预测供应数量 |
| 预测点数 | DECIMAL | **当前不使用**（先只看预测数量） |
| 数据刷新时间 | TIMESTAMP | 仅保存展示 |

业务规则：

- **聚合规则**：同一编码 + 同一计划日期（版本）+ 同一预测月份的多条记录，预测数量**求和**，得到该编码该版本的月度预测供应量。
- 表结构中其余字段需要保存，用作后续计算/展示。
- 示例：编码 302063459、计划日期 2026-08-24、预测所在年月 2026-09（预测从 9 月开始）；其中预测月份 2026-09 有两条记录（10、30），则该编码该版本 9 月预测数量 = 10 + 30 = **40**；其余月份（2026-10 ~ 2027-07）各一条直接取值。

### 8.3 表2 供应添加&使用剔除表（手工创建&维护）

表结构按示例定义（月份为动态列表头）：

| 字段 | 说明 |
|---|---|
| 供应/使用 | `供应` = 供应添加量：项目编码按外购编码和月份维度增加供应量；`使用` = 使用剔除量：项目编码按月份维度剔除使用量 |
| 项目编码 | 实际编码 |
| 外购编码 | 仅「供应」行使用；用于补充项目编码供应量的编码；每个月可能不同，同一个月可以有多个外购编码（计算时量相加） |
| 月份列（如 `2026-08`、`2026-09`…） | 按月维度维护供应添加/使用剔除量；多个月份作为表头，命名格式 `YYYY-MM` 表示对应年月数据 |

计算规则：

- **供应添加**：项目编码某月实际供应量 = 原预测供应量 + Σ 当月各外购编码添加量。示例：A1 每月预测供应量 500，2026-08 外购 A2 补 1000 → 实际供应 1500；2026-10 外购 A2 补 1000 + A3 补 1000 → 实际供应 2500。
- **使用剔除**：项目编码某月纯预测需求 = 原预测量 − 当月剔除量（剔除量可为独立供应需求，需从预测需求中扣减以保障独立需求满足）。示例：B1 某月预测量 3000，剔除 1000 → 纯预测量 2000。
- **综合 GAP 示例**：B1 纯需求 2000，A1 纯供应 1500 → GAP = 2000 − 1500 = **500**（仍缺 500）。

功能要求：该表需**前台界面维护数值**（按行增删改、按月份列维护），并构造模拟数据进行效果呈现；维护界面与导入导出的实现方案见 8.11。

### 8.4 表3 BOM 基表（BASE0007_ERP_ITEM_BOM_lky）

| 字段名 | 数据类型 | 说明 |
|---|---|---|
| id | BIGINT | **关键字段**：数据唯一键值 |
| 父项 | VARCHAR | **关键字段**：父项编码 |
| 子项 | VARCHAR | **关键字段**：子项编码 |
| 子项单位用量 | DOUBLE | **关键字段**：子项与父项的供应比例，如为 2 则需 2 个子项供应 1 个父项；用量小于 1 时小数点后取两位（四舍五入） |
| 子项供应类型 / 子项模板 / 父项供应类型 / 父项模板 | VARCHAR | 仅做展示 |
| 创建时间 | TIMESTAMP | 仅做展示 |

- 编码之间**不存在自环和循环依赖**（与 6.1 环检测兜底一致，正式数据预期 cycles=0）。
- 层级示例：C1 ←(2)— B1 ←(1)— A1，C1 ←(3)— A2，即制作 1 个 C1 需 3 个 A2、2 个 B1（等价 2 个 A1）。

### 8.5 表4 库存表（T_INVENTORY_PHOTO_WIDE_TABLE 库存拍照宽表）

| 字段名 | 数据类型 | 说明 |
|---|---|---|
| id | BIGINT | 唯一键值 |
| 创建日期 | DATE | **关键字段**：用于筛选数据（月初库存选当月 1 日，实时库存选当日） |
| 编码 | VARCHAR | **关键字段**：item 编码 |
| ERP子库 / 货位描述 / 货位 | VARCHAR | 仅做展示（货位描述为需要使用的展示字段） |
| 可用量 | DOUBLE | **关键字段**：库存量 |
| 子库类型 | VARCHAR | **关键字段**：预处理排除开头为 `生产-三品` 和 `备件-` 的数据 |
| 组织ID | DOUBLE | 仅做展示 |
| 产品大类 / 产品小类 / 产品族 | VARCHAR | 仅做展示 |
| 项目模板 | VARCHAR | **关键字段**：项目模板 |

预处理规则：

1. 排除「子库类型」以 `生产-三品` 或 `备件-` 开头的数据；
2. **月初库存**：筛选创建日期 = 计算月份当月 1 日的数据（如计算 2026-08 → 取 2026-08-01 创建的数据）；
3. **实时库存**：创建日期取当日。



### 8.7 表6 制造属性维护基表（t_sche_make_attribute_rt）

| 字段名 | 数据类型 | 注释 |
|---|---|---|
| org_id | numeric | 组织ID（仅展示） |
| part_no | nvarchar2 | **关键字段**：产品编码 |
| make_dept | nvarchar2 | **关键字段**：制造部门，用于区分产业 |
| life_cycle_state | nvarchar2 | 产品生命周期（仅展示） |
| part_category / part_subcategory / part_series | nvarchar2 | 产品大类/小类/系列（仅展示） |
| product_line / product_family | nvarchar2 | 产品线/产品簇（仅展示） |

- 本需求跨产业：找编码对应的上层编码时「找到哪一层为止」，需通过本表 make_dept 识别。
- **跨产业映射表**：哪些 make_dept 属于同一个产业，须作为**可配置信息**（前台可维护），**不得在后台写死**。
- 示例口径：「基础数字能源制造部」和「智能电动制造部」属于本产业，其他值均为非本产业（跨产业）。

### 8.8 供需计算对象：三种层级关系口径

基于表3 BOM 基表 + 表6 制造属性维护基表，每个编码的「供需计算对象」有三种分析口径。示例数据：A1、B1、C1 属基础数字能源制造部，B2 属 X1 部，C2 属 X2 部；链路 C1←B1←A1、C2←B2←A1。

1. **全量父子关系**：分析所有编码上下层之间的关系，如 A1-B1、A1-B2、B1-C1、B2-C2；
2. **跨产业供需关系**：对每个编码找两个对象——
   - ① 同一产业内最上层编码：只要父项仍属本产业就一直往上找，直到最顶层（如 A1 → C1）；
   - ② 跨产业第一个编码：上溯过程中第一个不属于本产业的父项即停止（如 A1 → B2）；
3. **子项-最顶层关系**：每个编码直接找到对应最上层编码，不关注是否跨产业（如 A1-C1、A1-C2、B1-C1、B2-C2）。

这三种关系，需要通过按钮视角切换查看供需计算值（使用同一个bom层级的网络图）

> 实现提示：BOM 父子关系与「编码 → 供需计算对象」关系变动很少，应分别缓存（对应 8.9 计算步骤中的缓存一/缓存二）。

### 8.9 供需计算流程（实际业务版）

1. **加载 BOM 基表**：缓存一 = 每个编码的父子项关系；缓存二 = 每个底层编码对应的「供需计算对象」（表3 + 表6，按 8.8 三种口径）；
2. **读取表1**：按 8.2 聚合规则获取使用方需求（编码 × 版本 × 预测月份）；
3. **叠加表2**：将供应添加量计入供应方预测量、使用剔除量从使用方预测量扣减，分别得到**纯供应需求预测**和**纯使用需求预测**；
4. **读取表4**：按 8.5 预处理规则获取月初库存，每个编码节点加入库存；
5. **计算 GAP**：通过步骤 1 加载的 BOM 层级关系，按 8.8 三种口径计算供需匹配 GAP（父子项编码间 / 跨产业编码间 / 子项与最顶层编码间）。

### 8.10 数据输入与结果展示（本期范围）

数据输入：

- 表1、表3、表4、表6 为数据库表；**第一个版本先做 Excel 导入功能**（构造模拟数据进行效果呈现），不直接对接数据库——复用 3.4 数据管理页的导入能力，需扩展 `TABLE_SCHEMAS` 与 `detectTable` 以识别本章实际表头；
- 表2 手工维护：前台界面维护数值（实现方案见 8.11），并构造模拟数据呈现；
- 表5 本期不需要（见 8.6）。

结果展示（V1.2：以下展示统一汇聚在「供应网络总览」页的「供需网络（三口径）」卡片内，不再独立页签）：

1. **网络图**：展示各编码的层级关系和供需配比、每个编码的预测量、GAP 信息（位于 3.1 总览页原网络图卡片上方，支持 8.8 三口径切换与编码供需清单联动，节点详情含月度预测量/纯供应/纯需求/GAP）；
2. **跨产业供需关系**：8.8 口径 2 的跨产业供需关系作为该卡片内「跨产业供需视图 · 累计跨度分析」独立区块展示，需展示预测**累计 N 个月**跨度的数据（N 可选）。

### 8.11 手工维护表的维护界面与导入导出实现方案

适用范围：表2 供应添加&使用剔除表（**本期实现**）、表5 库存水位维护基表（本期暂缓，方案预留，见 8.11.6）。

#### 8.11.1 功能入口与布局

- 数据管理页（3.4）新增「手工维护表」区域，位于批量导入区与单表管理卡之间；每张手工表一张卡片：表名、说明、当前行数、「维护」「导入」「导出」「模板」按钮；
- 点击「维护」打开抽屉式/全屏编辑界面（覆盖层，不离开数据管理页）。

#### 8.11.2 表2 维护界面（宽表编辑）

- **表格形态**：固定列（供应/使用、项目编码、外购编码）+ 动态月份列（表头格式 `YYYY-MM`）；
- **月份列管理**：「+ 添加月份」按钮新增列（校验 `YYYY-MM` 格式并去重）；月份列建议与当前预测版本跨度对齐（表1 预测所在年月起 11 个月）；月份列可删除（二次确认，删除即清除该月全部单元格）；
- **行操作**：新增行、行内编辑、删除行（二次确认）；「供应/使用」为下拉枚举；供应行外购编码必填，使用行外购编码置灰并清空；
- **校验规则**：项目编码必填（建议存在于编码全集，不存在时仅告警不拦截）；月份单元格为数值且 ≥ 0（空按 0 处理）；同一（供应/使用 + 项目编码 + 外购编码）组合仅允许一行；
- **保存生效**：保存 → 写入存储 → 触发 `afterDataChange()` 全量重算（对应 8.9 步骤 3 的纯供应/纯使用需求预测）。

#### 8.11.3 存储模型（宽表展示、长表存储）

前台展示为宽表（月份为列），持久化统一为**长表**（月份为行），前端负责 pivot/unpivot 转换：

```sql
CREATE TABLE supply_adjust (            -- 表2 供应添加&使用剔除
  id            BIGINT PRIMARY KEY AUTO_INCREMENT,
  direction     VARCHAR(8)   NOT NULL,  -- 供应 / 使用
  item_code     VARCHAR(64)  NOT NULL,  -- 项目编码
  purchase_code VARCHAR(64)  DEFAULT '',-- 外购编码（使用行为空）
  month         CHAR(7)      NOT NULL,  -- YYYY-MM
  qty           DECIMAL(18,3) NOT NULL DEFAULT 0,
  updated_at    DATETIME,
  UNIQUE KEY uk_adjust (direction, item_code, purchase_code, month)
);

CREATE TABLE water_level (              -- 表5 库存水位，本期暂缓
  id              BIGINT PRIMARY KEY AUTO_INCREMENT,
  product_package VARCHAR(128),         -- 产品包，仅展示
  code            VARCHAR(64) NOT NULL,
  water_30d       DECIMAL(18,3) NOT NULL, -- 水位参考值(30天)
  UNIQUE KEY uk_water (code)
);
```

#### 8.11.4 导入导出

- 复用 3.4 导入框架：`TABLE_SCHEMAS` 扩展 `supply_adjust`（中文表头：供应/使用、项目编码、外购编码 + 动态月份列）；`detectTable` 以固定三列命中识别（月份列不参与命中计数）；
- **导入语义**：整表替换（与 Demo 其他表一致）；宽表（月份为列）与长表（月份为行）两种格式均接受，导入时统一转长表存储；
- **导出**：中文表头 CSV（带 BOM 头，字段引号转义），默认宽表格式；「模板」下载带 1 行示例的 CSV；
- 校验与错误处理遵循 8.11.2 与 7.5（严格模式整表拒绝 + 错误明细）。

#### 8.11.5 API 扩展（追加至 7.3 节清单）

| # | 方法 | 路径 | 说明 |
|---|---|---|---|
| 24 | GET | `/api/manual/supply-adjust` | 查询全部（长表数组，前端 pivot 为宽表） |
| 25 | POST | `/api/manual/supply-adjust/rows` | 新增行（body 为长表行数组，支持一次提交整行各月份） |
| 26 | PUT | `/api/manual/supply-adjust/rows/{id}` | 编辑行 |
| 27 | DELETE | `/api/manual/supply-adjust/rows/{id}` | 删除行 |
| 28 | POST | `/api/import/supply_adjust` | 整表替换导入（复用 #19 通道，宽/长表均可） |
| 29 | GET | `/api/export/supply_adjust?format=wide\|long` | 导出（复用 #20 通道，默认宽表） |

- Demo（`LocalProvider`）：内存新增 `supply_adjust` 表 + 模拟种子数据（即 8.3 示例：供应 A1←A2/A3、使用 B2），维护/导入/导出方法与其他表一样包装为 async；切换 `RemoteProvider` 时仅替换实现，接口不变；
- 编辑/导入成功后数据版本号 +1，触发全量重算（同 7.4 缓存失效策略）。

#### 8.11.6 表5 预留说明

本期不做预警推送，表5 不上维护卡片；其维护界面与导入导出走与表2 完全同构的框架（`/api/manual/water-level`），后续版本仅需启用卡片并接入水位预警逻辑。

---

## 9. 非功能性需求

| 类别 | 要求 |
|---|---|
| 性能 | 数据规模假设：编码 ≤ 3 万、BOM 边 ≤ 15 万、MO ≤ 50 万行、预测 ≤ 20 万行（对应引擎注释中的 3 万编码设计规模）；服务端性能目标见 7.4；前端 >1000 节点 LOD 降级 |
| 浏览器兼容 | Chrome / Edge 最近两个大版本（Demo 使用 ES2017+、Fetch、URLSearchParams、Float64Array、requestAnimationFrame）；不保证 IE |
| 安全 | 正式版需加鉴权（建议 JWT/Session，全部 `/api/*` 需登录）；权限建议两级：只读（看板/报告/详情）与 管理（导入/导出/重置/配置保存/推演）；接口入参服务端校验防注入；上传文件类型与大小白名单 |
| 可扩展性 | Provider 抽象保证前后端解耦；报告页「建议动作」已预留 AI 解读接口（topAnalysis 输出标准 JSON，可直接送 LLM 增强）；阈值全部配置化；新增基表只需扩展 TABLE_SCHEMAS + 引擎索引 |
| 可靠性 | 导入事务化；计算失败不污染缓存；前端全局 error 兜底提示（已有） |
| 可维护性 | 正式版建议将单文件拆分为模块（引擎/Provider/UI/图表），vendor 库改 npm/CDN 管理 |

---

## 10. 附录

### 10.1 枚举值对照表

**短缺分级 `shortage`**：`severe` 严重短缺 / `moderate` 中等短缺 / `light` 轻微短缺 / `balanced` 均衡
**过剩分级 `excess`**：`severe` / `moderate` / `light` / `balanced`（同上语义）
**交期风险 `delayLevel`**：`high` 高 / `moderate` 中 / `low` 低 / `nodata` 无历史数据
**综合风险 `composite`**：`high` 高 / `moderate` 中 / `low` 低（图谱中另用 `nodata`、`order` 作展示态）
**预测对账 `forecastStatus`**：`matched` 预测匹配 / `forecast_under` 预测不足 / `forecast_over` 预测过剩 / `no_forecast` 无自身预测 / `no_topdown` 无上层传导 / `no_topdown_has_forecast` 仅自身预测
**计划对账 `planStatus`**：`severe_shortage` / `moderate_shortage` / `light_shortage` / `balanced` 计划匹配 / `light_excess` / `moderate_excess` / `severe_excess`
**加工地健康度**：`健康`（riskNodes=0）/ `关注`（≤2）/ `预警`（>2）
**需求口径 `demand_mode`**：`order_forecast` 订单+预测 / `order_only` 仅订单(PTO) / `forecast_only` 仅预测
**层级 level**：-1 订单 / 0 成品 / 1、2 半成品 / ≥3 原材料（采购件）
**表名键**：`bom` BOM结构表 / `material` 物料主数据表 / `inventory` 库存表 / `mo` 天级计划产出表 / `forecast_w` 周级预测表 / `forecast_m` 月级预测表 / `orders` 订单需求表(PTO)
**模拟场景 type**：`质量异常` / `加工地停工`
**供应/使用（表2）**：`供应` 供应添加量 / `使用` 使用剔除量
**供需计算对象口径**：`父子关系` / `跨产业`（本产业最顶层 + 跨产业第一个编码）/ `最顶层`（不区分产业）
**库存口径（表4）**：`月初库存`（创建日期=当月1日）/ `实时库存`（创建日期=当日）

### 10.2 Demo 与正式实现差异说明

| 项 | Demo 现状 | 正式实现要求 |
|---|---|---|
| 数据来源 | `buildSeed()` 用 mulberry32(20260813) 确定性伪随机生成 62 编码 / 118 BOM 边的演示数据集（覆盖独家+高交期、未分配加工地、无历史、各级短缺/过剩、跨产业共用料等场景） | 由数据库真实数据替换；种子数据仅作测试夹具 |
| 计算位置 | 浏览器内 Engine 纯 JS 同步计算 | 服务端引擎等价实现（算法不变），前端改 `RemoteProvider` |
| vendor 库 | ECharts/G6/SheetJS 压缩内嵌于 HTML | npm 依赖或 CDN；G6 与 ECharts 图渲染二选一（Demo 主渲染已是 ECharts，`highlightPaths` 中 G6 兼容代码可清理） |
| 持久化 | 无，刷新即还原 | 数据库持久化 + 数据版本管理 |
| 导入语义 | 整表替换，无校验、无事务 | 服务端校验 + 事务 + 错误明细（7.5） |
| 配置保存 | 内存合并，刷新失效 | `app_config` 表持久化 |
| 重置 | 恢复种子数据 | 建议改为恢复基线快照 |
| 鉴权 | 无 | 登录 + 两级权限 |
| T0 基准 | 前端本地当天 0 点 | 服务端统一基准日（注意时区，建议东八区固定） |
| 数据管理页 | 导入卡说明文案缺 orders 表（`desc` 映射遗漏） | 正式版补齐 |
| 配置项 | `exclusive_stable_weeks` 定义未使用 | 实现或移除 |
| material 字段 | `safety_days`、`moq` 仅展示不参与计算；`status`（mo/orders）不参与计算 | 若业务需要可纳入 netting（如安全库存天数抵扣水位），属需求扩展，需评审 |
| 日期过滤 | bom.eff_date/exp_date 未参与计算（默认全有效） | 正式版应按 T0 过滤生效中的 BOM 行，属需求扩展，需评审 |
| 数据模型 | 7 张演示基表（第 5 章）：相对周/月预测、单快照库存、产业为物料主数据字段 | 以第 8 章实际数据表为准：版本化月度预测（计划日期×预测月份聚合）、按日拍照库存（子库类型过滤+月初/实时口径）、制造部门（make_dept）识别产业且跨产业映射可配置；新增供应添加/使用剔除表（前台维护）与库存水位表（本期暂缓）；新增跨产业供需独立展示界面 |
